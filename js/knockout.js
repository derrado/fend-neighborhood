// var ViewModel = function() {
    // var self = this;

    // for (var i = 0; i < locations.length; i++) {
    //     self.items.push(new Venue(locations[i]));
    // }

    // self.filter = ko.observable('');
    // self.items = ko.observableArray(locations);

    // self.visibleLocations = ko.computed(function() {
    //     var filter = self.filter()
    //     let arr = self.items().filter(function(i) { return i.title.indexOf(filter) > -1; });
    //     return arr;
    // });



    // self.displayMarkersOnMap
// };


